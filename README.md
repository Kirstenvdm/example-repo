# Example Repo

This project is a Python program that reads data from a text file called DOB.txt.

The program separates each person's full name and birthdate into two different lists and then prints the names and birthdates separately.